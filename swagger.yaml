info: {}
paths:
  /products:
    get:
      description: Returns a list of products
      operationId: GetProducts
      responses:
        "200":
          $ref: '#/responses/productsReponse'
      tags:
      - products
swagger: "2.0"
